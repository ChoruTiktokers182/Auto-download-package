# Auto-download-package
```
apt update && apt upgrade -y && apt install -y git && git clone https://github.com/ChoruTiktokers182/Auto-download-package && cd Auto-download-package && chmod +x install.sh && bash install.sh

```
